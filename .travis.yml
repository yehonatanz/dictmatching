language: python
python:
  - "2.7"
  - "3.4"
  - "3.5"
  - "3.6"
  - "3.7-dev"
before_install:
  - pip install pytest pytest-cov coveralls
install:
  - pip install -e .
script:
  - pytest -vvv --cov=dictmatching
after_success:
  - coveralls
